import React from 'react';

import { Container, H1 } from './';

export default class SearchFilters extends React.PureComponent {
  render() {
    return (
      <Container testID="SearchFilters">
        <H1>SearchFilters</H1>
      </Container>
    );
  }
}
